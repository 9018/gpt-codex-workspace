{
  "name": "gptwork-mcp-backend",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/cli.mjs",
    "test": "node --test --test-reporter=dot 'test/**/*.test.mjs'",
    "test:clean": "node test/helpers/run-clean.mjs",
    "test:tap": "node --test 'test/**/*.test.mjs'"
  }
}
